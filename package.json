{
  "name": "glacierfarm",
  "version": "1.0.0",
  "description": "Portable Cold Storage Solutions for Farmers",
  "private": true,
  "scripts": {
    "dev": "npm --prefix client run dev",
    "build": "npm --prefix client install && npm --prefix client run build && npm --prefix netlify/functions install",
    "start": "npm --prefix client run preview"
  },
  "keywords": [
    "cold storage",
    "farming",
    "agriculture"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "mongoose": "^8.20.0"
  }
}
